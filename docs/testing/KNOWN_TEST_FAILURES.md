# Known Test Failures

As of 2026-05-05, there are currently no known baseline test failures in this repository.

CI expectations are:
- `dotnet restore` passes.
- `dotnet build` passes.
- `dotnet test` passes (blocking).
- Frontend `npm run build` passes.
